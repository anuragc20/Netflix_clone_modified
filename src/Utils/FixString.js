// export const FixString = (string) => {
//   return string.split(" ").join("").toLowerCase().replace(/[^a-z0-9]/g, "");
// }

//Not getting...exactly why this is used
export const FixString = (string) => {
  return string.split(" ").join("").toLowerCase().replace(/[^a-z0-9]/g, "");
}